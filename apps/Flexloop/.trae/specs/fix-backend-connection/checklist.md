# 前后端连接修复检查清单

- [x] 前端 DeepResearch 客户端 baseURL 配置正确为 `http://localhost:8000/api/v1`
- [x] 后端 CORS 配置允许 `http://localhost:5175` 跨域访问 (当前 `allow_origins=["*"]` 已允许所有来源)
- [x] 所有 API 端点路径与后端路由定义匹配（包含 `/api/v1` 前缀）
- [x] API 密钥能够正确从前端传递到后端
- [x] 导入路径错误已修复 (`../errors` → `../services/errors`)
- [ ] 后端能够成功接收前端发送的请求（需要浏览器测试）
- [ ] 前端能够正确接收后端返回的响应（需要浏览器测试）
- [ ] SSE 流式进度更新能够正常工作（需要浏览器测试）
- [x] 构建过程没有 TypeScript 错误 ✅ 构建成功
- [x] 开发服务器启动正常，没有运行时错误 ✅ 服务器运行在 http://localhost:5175/
- [ ] 实际测试能够完成一次完整的深度研究请求/响应流程（需要浏览器测试）
